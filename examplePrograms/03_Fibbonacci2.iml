program fibbonacci()
    global
    proc recurse(in var x: int32, in var y: int32)
        do
            debugout y;
            y := y + x;
            call recurse(y , x)
    endproc
    do
        call recurse(1,1)
endprogram