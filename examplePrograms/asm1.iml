program test1(a:int32,tup d:(int32,int32,int32),tup q:(int32,int32,int32))
global
 

  proc test(out ref tup t:(int32,int32,int32),out ref tup tt:(int32,int32,int32))
    do

	  t:=tup(1,1,t[2]);


      if t=tt
      then
      	debugout 1337
      else
      	debugout 122
      endif  
  endproc
  
  
  do
    d:=tup(1,2,3);
	q:=d;
	call test(d,q)
	
endprogram
