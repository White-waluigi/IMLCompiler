program test1(a :bool,b: int32,c:int32,   tup q:(int32, int32,int32), tup d:(int32,int32,int32))
global
 
  proc fib(in ref aa:int32,in s:int32, in t:int32)
	do  	
		if aa = 0 || aa = 1
		then 
			aa:=1
		else
			s:=aa-1;
			call fib(s,c,c);
			t:=aa-2;
			call fib(t,c,c);
			aa:=t+s
		endif
		
		
		
  endproc;
  proc test(out tup t:(int32,int32,int32),in tup tt:(int32,int32,int32))
    do

      if t>=tt
      then
      	debugout 1337
      else
      	debugout 122
      endif  
  endproc
  
  
  do
  	d:=tup(6756,234,466);
	q:=d;
	call test(d,q)
	
endprogram
