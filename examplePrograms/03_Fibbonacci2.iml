program fibbonacci()
    global
    proc recurse(in var x: int32, in var y: int32)
        do
            debugout y;
            y := y + x;
            if x < 1000 then
                call recurse(y , x)
            else
                skip
            endif
    endproc
    do
        call recurse(1,1)
endprogram